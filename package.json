{
  "name": "myartverse",
  "version": "0.0.5",
  "license": "Apache-2.0",
  "private": true,
  "packageManager": "npm@10.9.7",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "dotenv -- turbo dev --filter=@mav/web-client --no-daemon",
    "dev:desktop": "cd ./apps/desktop && tauri dev",
    "build": "turbo run build --filter=@mav/web-client",
    "build:web": "turbo run build --filter=@mav/web-client",
    "build:all": "turbo build --concurrency 2",
    "start:web": "npm run start --workspace=@mav/web-client",
    "format": "biome format",
    "format:write": "biome format --write",
    "lint": "biome check",
    "lint:write": "biome check . --apply",
    "knip": "knip"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^7.4.2",
    "knip": "^5.16.0",
    "turbo": "^2.9.16"
  },
  "overrides": {
    "@types/react": "19.0.10",
    "@types/react-dom": "19.0.4",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "dependencies": {
    "swiper": "12.2.0"
  }
}
