{
  "name": "myfursona",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "homepage": "https://github.com/MyFursona-Project/MyFursona",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=website --no-daemon",
    "dev:client": "cd apps/client && yarn tauri dev",
    "build": "turbo build --filter=website",
    "build:client": "cd apps/client && yarn tauri build",
    "build:all": "turbo build",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "prettier": "^3.0.2",
    "turbo": "^1.10.13",
    "husky": "^8.0.0"
  },
  "engines": {
    "node": ">= 18.0.0",
    "npm": ">= 8.0.0"
  }
}
