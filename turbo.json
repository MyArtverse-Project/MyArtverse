{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "check-types": {},
    "deploy": {
      "dependsOn": ["lint", "build"]
    },
    
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
